[project]
name = "audio-python-watermark-native"
version = "0.1.0"
description = "Detect Speechify's audio watermark via the Speechify REST API."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
    "python-dotenv>=1.0.0",
]
