[project]
name = "audio-python-multilingual-native"
version = "0.1.0"
description = "Synthesize non-English speech using the Speechify REST API directly (no SDK)."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
    "python-dotenv>=1.0.0",
]
