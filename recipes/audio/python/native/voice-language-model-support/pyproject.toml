[project]
name = "audio-python-voice-language-model-support-native"
version = "0.1.0"
description = "Discover which model + language combinations a voice supports via the Speechify REST API."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
    "python-dotenv>=1.0.0",
]
