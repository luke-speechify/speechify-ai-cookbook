[project]
name = "audio-python-speech-marks"
version = "0.1.0"
description = "Generate WebVTT captions from word-level speech marks with the Speechify TTS API."
requires-python = ">=3.10"
dependencies = [
    "speechify-api>=4.0.0",
    "python-dotenv>=1.0.0",
]
