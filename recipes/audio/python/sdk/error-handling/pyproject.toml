[project]
name = "audio-python-error-handling"
version = "0.1.0"
description = "Handle Speechify API errors correctly with the Speechify SDK."
requires-python = ">=3.10"
dependencies = [
    "speechify-api>=4.0.0",
    "python-dotenv>=1.0.0",
]
