[project]
name = "audio-python-ssml-emotion"
version = "0.1.0"
description = "Control emotion, pitch, rate, pauses & emphasis via SSML with the Speechify TTS API."
requires-python = ">=3.10"
dependencies = [
    "speechify-api>=4.0.0",
    "python-dotenv>=1.0.0",
]
