[project]
name = "audio-python-streaming"
version = "0.1.0"
description = "Stream synthesized audio to a file as it is generated with the Speechify TTS API."
requires-python = ">=3.10"
dependencies = [
    "speechify-api>=4.0.0",
    "python-dotenv>=1.0.0",
]
