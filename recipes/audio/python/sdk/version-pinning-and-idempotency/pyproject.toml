[project]
name = "audio-python-version-pinning-and-idempotency"
version = "0.1.0"
description = "Pin the API version and replay a POST idempotently with the Speechify SDK."
requires-python = ">=3.10"
dependencies = [
    "speechify-api>=4.0.0",
    "python-dotenv>=1.0.0",
]
