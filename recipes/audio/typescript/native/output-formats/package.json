{
  "name": "tts-output-formats-native",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Control the exact codec, sample rate, and bitrate via output_format using the Speechify TTS REST API directly (no SDK).",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
